# =============================================================================
# Main Context Logging Configuration
# =============================================================================

# Write warnings and errors to both file and stderr
error_log /usr/local/openresty/nginx/logs/error.log warn;
error_log /dev/stderr warn;